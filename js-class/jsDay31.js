// Working with queues.
// Examples: Uh its a line. Do you need examples?
// Problems with data structures.
// Lined lists.
// Need to keep track of my stuff and who comes next.

// We need to keep track of only the first one.
// They should all have Nodes ( look into this )

function Node( data ){
    this.next = null;
    this.data = data;
}

function LinkedList(){
    var head = null;
    this.length = 0;
    this.insert = function( data ){

    };
    this.delete = function( id ){

    };
    this.find = function( name ){

    };
}